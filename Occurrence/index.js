let numbers = [ 5, 10, 15, 15, 20, 20, 20, 25, 25, 30, 35, 35, 35, 40 ]
let target = 35;

function occurrenceNumber (array, target){
    let start = 0;
    let end = array.length - 1;
    let mid;
    let count = 1;

    while(start <= end){
        mid = Math.floor((start + end ) / 2);

        if(array[mid] === target){
            
        }
    }


}

